<%= link_to 'add comment', new_event_comment_path(@event) %>

<% if @near_users.include?(current_user) %>
	<% if @current_user.approved_events.include?(@event) %>
		<%= link_to 'approve',  event_approve_index_path(@event), :method => :post, :remote => true %>
	<% end %>
<% end %>